server:
  port: 8084

spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  datasource:
    url: jdbc:mysql://localhost:3306/product
    username: root
    password: password
